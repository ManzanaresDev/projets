{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "build": "cd frontend && npm install && npm run build",
  "start": "cd backend && npm install && node server.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-cookie": "^8.0.1"
  },
  "devDependencies": {
    "vite": "^6.3.2"
  }
}
